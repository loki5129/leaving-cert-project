Initialize a dictionary(types)
extract keys and store them in a list
ilerate through each line of the csv
for each line 
 convert to lowercase
 ilerate through the list of keys
 for each key
  if key in line
   dictonary of key increase by 1






function takes in a string
 string is turned into a string
 string is turned lowercase
 removes all the string that is not the level
 returns the level

 